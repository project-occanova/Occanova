import Link from 'next/link';
import {LayoutDashboard,Store,Mail,Settings2,Layers,ArrowUpRight,ClipboardList} from 'lucide-react';
import {Logout} from './forms';
export function Workspace({admin=false,children,email}:{admin?:boolean;children:React.ReactNode;email:string}){const base=admin?'/admin':'/dashboard';return <div className={`workspace${admin?' admin-workspace':''}`}><aside className="sidebar"><Link href={base} className="sidebar-brand">OCCANOVA<small>{admin?'ADMIN STUDIO':'VENDOR STUDIO'}</small></Link><nav aria-label="Workspace navigation"><Link href={base}><LayoutDashboard size={18}/>Overview</Link><a href={base+(admin?'#vendors':'#profile')}><Store size={18}/>{admin?'Vendor review':'Business profile'}</a><a href={base+'#enquiries'}><Mail size={18}/>Enquiries</a>{admin&&<><a href={base+'#taxonomy'}><Layers size={18}/>Categories & locations</a><a href={base+'#activity'}><ClipboardList size={18}/>Activity log</a></>}<a href={base+'#account'}><Settings2 size={18}/>Account</a></nav><div className="sidebar-bottom"><Link href="/">Visit website<ArrowUpRight size={16}/></Link><Logout redirectTo={admin?'/admin/login':'/login'}/></div></aside><div className="workspace-content"><header className="workspace-header"><span>{admin?'Administration':'Your business, at a glance'}</span><span>{email}</span></header><main id="main" className="workspace-main">{children}</main></div></div>;}
