import { z } from 'zod';
const phone=z.string().regex(/^\+?[0-9 ()-]{10,20}$/,'Enter a valid phone number');
export const registerSchema=z.object({email:z.string().email().max(160).transform(x=>x.toLowerCase()),phone,password:z.string().min(10).max(128),consent:z.literal(true)});
export const enquirySchema=z.object({vendorId:z.string().max(100),name:z.string().trim().min(2).max(100),contact:z.string().trim().max(160).refine(x=>z.string().email().safeParse(x).success||phone.safeParse(x).success,'Enter a valid email or phone'),city:z.string().trim().min(2).max(100),date:z.string().regex(/^\d{4}-\d{2}-\d{2}$/).refine(x=>!isNaN(Date.parse(x))&&x>=new Date().toISOString().slice(0,10),'Choose today or a future date'),service:z.string().trim().min(2).max(100),message:z.string().trim().min(10).max(2000),consent:z.literal(true),website:z.string().max(0).optional()});
export const profileSchema=z.object({name:z.string().trim().min(2).max(100),owner:z.string().trim().min(2).max(100),category:z.string().min(2).max(80),service:z.string().min(2).max(100),city:z.string().min(2).max(80),experience:z.coerce.number().int().min(0).max(100),description:z.string().trim().min(20).max(3000),summary:z.string().trim().min(10).max(180),price:z.coerce.number().min(0).max(100000000),phone,whatsapp:phone,email:z.string().email().max(160),image:z.string().max(500).optional().default(''),gallery:z.array(z.string().max(500)).max(12).optional().default([]),documents:z.array(z.string().max(500)).max(5).optional().default([])});
